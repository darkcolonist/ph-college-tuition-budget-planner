VITE_APP_TITLE=EduBank PH
VITE_API_URL=http://localhost:8787

# Toggle for local mock data (true/false)
# If set to true, the app will use local mock data instead of calling the worker.
# Default is true if not specified.
VITE_USE_LOCAL_MOCK=true
